CREATE DATABASE Biblioteca;

CREATE TABLE lectores 
(
    clave_lector int (10) PRIMARY KEY,
	nombre char (20) not null,
    ap_paterno varchar (40) NOT null,
    ap_materno varchar (40) NOT null
    );
    
CREATE TABLE libros
(
    clave_libros int (10) PRIMARY KEY,
    titulo char (50) NOT null,
    paginas int NOT null
    );
CREATE TABLE prestamos
(
    id_prestamo int (10)
    );
     INSERT INTO lectores (nombre,ap_paterno,ap_materno) VALUES (raul,martines,gonzalez),(miguel,suares,martinez),
    (Noe,perez,lopez);
    
   	INSERT INTO libros(titulo,paginas)
    VALUES (la perla,255),(fundamentos de programacion,877),
    (programar en java,500);
    
    SELECT*FROM libros;
    SELECT*FROM lectores;
    
    alter table prestamos 
    add clave_libros int;
    
    ALTER table prestamos 
    ALTER column clave_lectoresint not null;

USE pruebas
drop TABLE if EXISTS decimales;
create TABLE decimales(
    precio float(6,2) ZEROFILL
    ); 
    
INSERT into decimales VALUES (20 99);
INSERT into decimales VALUES (30);
INSERT into decimales VALUES (199 994);
INSERT into decimales VALUES (199 995);
SELECT* from decimales;
